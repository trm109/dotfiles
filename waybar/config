{
    "layer": "top",
    "height": 40,
    "fixed-center":true,
    "modules-left": ["custom/launcher","cpu","memory","network","custom/vpn","custom/media"],
    "modules-center": ["wlr/workspaces"],
    "modules-right": ["custom/supergfx","backlight","custom/wallpaper","pulseaudio","battery","clock","custom/power"],
    
    //To do;
    // 1. Add Supergfxctl support.
    // 2. Vpn support.
    // 3. Powermanagement support (low-med-high)
    "custom/supergfx": {
    	"format": "GPU {}",
    	"exec": "~/.config/waybar/scripts/supergfx/gpu_status_listener.sh",
    	"return-type":"json"
    },
    "wlr/workspaces": {
        "format": "{icon}",
        "on-scroll-up": "hyprctl dispatch workspace e+1",
        "on-scroll-down": "hyprctl dispatch workspace e-1",
        "on-click": "activate"
    },
    "pulseaudio": {
        "tooltip": false,
        "scroll-step": 5,
        "format": "{icon} {volume}%",
        "format-muted": " ",
        "on-click":"pactl set-sink-mute @DEFAULT_SINK@ toggle",
        "format-icons": {
            "default": ["", "", ""]
        }
    },
    "network": {
        "format-wifi": "  {essid} ({signalStrength}%)",
        "format-ethernet": "󰈀 {ifname}",
        "tooltip-format": "{essid} via {gwaddr}({ifname})",
        "format-disconnected": "󰖪 Disconnected",
        "on-click": ""
    },
    "backlight": {
        "tooltip": false,
        "format": " {}%",
        "interval":1,
        "on-scroll-up": "light -A 5",
        "on-scroll-down": "light -U 5"
    },
    "battery": {
        "states": {
            "good": 95,
            "warning": 30,
            "critical": 20
        },
        "format": "{icon} {capacity}%",
        "format-charging": "󰂄 {capacity}%",
        "format-plugged": " {capacity}%",
        "format-alt": "{time} {icon}",
        "format-icons": ["󰁺", "󰁻", "󰁼", "󰁽", "󰁾", "󰁿", "󰂀", "󰂁", "󰂂", "󰁹"]
    },
    "tray":{
        "icon-size":18,
        "spacing": 10
    },
    "clock": {
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>",
        "format": "{:%a %b %d, %I:%M %p}"
    },
    "cpu": {
        "interval": 15,
        "format": " {usage}%",
        "max-length": 10
    },
    "memory": {
        "interval": 30,
        "format": " {}%",
        "max-length": 10
    },
    "custom/media": {
        "format": "{icon}{}",
        "return-type": "json",
        "format-icons": {
            "Playing": " ",
            "Paused": " ",
        },
        "max-length":50,
        "exec": "playerctl -a metadata --format '{\"text\": \"{{playerName}}: {{artist}} - {{markup_escape(title)}}\", \"tooltip\": \"{{playerName}} : {{markup_escape(title)}}\", \"alt\": \"{{status}}\", \"class\": \"{{status}}\"}' -F",
        "on-click": "playerctl play-pause",
    },
    "custom/spotify": {
        "format": " {}",
        "escape": true,
        "return-type": "json",
        "max-length": 40,
        "on-click": "playerctl -p spotifyd play-pause",
        "on-click-right": "killall spotifyd",
        "smooth-scrolling-threshold": 10,
        "on-scroll-up" : "playerctl -p spotifyd next",
        "on-scroll-down" : "playerctl -p spotifyd previous",
        "exec": "$HOME/bin/mediaplayer.py 2> /dev/null",
        "exec-if": "pgrep spotifyd"
    },
    "custom/vpn": {
        "format": "VPN ",
        "exec": "echo '{\"class\": \"connected\"}'",
        "exec-if": "[[ $(curl --silent https://am.i.mullvad.net/connected | grep -c \"not connected\") == 0 ]]",
        "return-type": "json",
        "interval": 5
    },
    "custom/launcher":{
        "format": " ",
        "on-click": "bash ~/.config/rofi/launcher/launcher.sh",
        "on-click-right": "killall rofi"
    },
    "custom/power":{
        "format": " ",
        "on-click": "bash ~/.config/rofi/powermenu/powermenu.sh",
    },
    "custom/updates":{
        "format": " {}",
        "exec": "checkupdates | wc -l",
        "exec-if": "[[ $(checkupdates | wc -l) != 0 ]]",
        "interval": 3600,
        "on-click": "alacritty -e paru -Syu && notify-send 'paru' 'The system has been updated' -i dialog-information-symbolic"
    }
}
